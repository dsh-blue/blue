# `@dsh-blue-example/ui-gallery`

Ordinary Cordis plugin injecting `bluePanes`. It registers one static
right-lane gallery built only from public `@dsh-blue/blue-ui` nodes and
degrades to the bottom lane when narrow. Keep its snapshots static and the
package opt-in.
