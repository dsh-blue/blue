# `@dsh-blue-example/right-inspector`

Ordinary Cordis plugin injecting `bluePanes`. It registers one passive right
inspector using public wire nodes and the user kit. Core owns right-to-bottom
degradation, layout, rendering, and width truth. Keep it opt-in.
