# `@dsh-blue-example/header`

Ordinary Cordis plugin injecting `bluePanes`. It registers one passive header
pane, uses the published user kit, hides on narrow layouts, and owns no mutable
session state. Keep it opt-in and outside the product bundle.
