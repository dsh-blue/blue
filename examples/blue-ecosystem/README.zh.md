# Blue 生态示例 Bundle

这是一个 opt-in composition bundle：安装共享用户 Kit，并通过六条 Cordis row
激活全部六个可运行示例。

```sh
dsh plugin --profile blue-examples add @dsh-blue-example/blue-ecosystem
```

请先在同一 profile 安装 Blue。本验证 bundle 不进入 Blue 默认产品组合或发布集。
