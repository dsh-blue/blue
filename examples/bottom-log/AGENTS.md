# `@dsh-blue-example/bottom-log`

Ordinary Cordis plugin injecting `bluePanes`. It contributes one bounded
passive bottom pane with no timer, subscription, background task, or duplicate
state source. Keep it opt-in.
