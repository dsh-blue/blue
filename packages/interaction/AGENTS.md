# @dsh-blue/blue-interaction — agent notes

Implementation detail for this package (the user-facing surface is `README.md`/`README.zh.md`). Repo-wide conventions live in the root [AGENTS.md](../../AGENTS.md); decisions cited as Dxx are in [docs/blue-decisions.md](../../docs/blue-decisions.md). Peer+dev dep on the harness line: `@deepseek-ai/dsh-attachment`.

## Editor and hint line

The bottom input editor (`src/input-plugin.ts`) is the pi-tui Editor behind `ctx.blueComponents.createEditor` plus a separate hint-line component. The editor mounts with `paddingX: 4` and the `>` prompt symbol (feeding the core rounded-box chrome); slash-prefixed input recolors the frame `primary` and anything else restores the neutral border (onChange chain). The hint line carries only its transient tiers — one-shot notices and slash discovery in `muted`; an empty notice clears the row instead of retaining an empty styled component. Command result text is flattened across CR/LF boundaries with ` · ` before width truncation: one component row may never contain embedded physical terminal lines (the `/goal` duplicate editor/footer regression). (A persistent key-affordance row was retired by the S15 dogfood verdict: kimi teaches affordances through the footer's rotating tips, which already cover every fragment the row carried.)

## Commands and aliases

`src/commands-plugin.ts` registers `/quit` `/resume` `/new` `/fork` `/sessions` `/help` `/init` (`/new`/`/fork` emit the app layer's `blue/request-new`/`blue/request-fork`; `/sessions` is a persistence-list picker panel; `/help` enumerates `commands.list` + `keymap.list()`). `/theme` (`src/theme-switch.ts`) hot-swaps the `blueTheme` provider via `ctx.registry.delete` + `ctx.plugin`, restoring dark on mount failure; the `/theme` listing marks the live palette with the shared `← current` vocabulary (the selected settings-list row styles in `primary`, not `accent`).
`/trace` (`src/trace-command.ts`) is a read-only execution timeline over the
official `ctx.sessionQuery` service. It uses one `readSession()` snapshot plus
the official `buildSessionEventRecords()` projection for the initial timeline,
and calls `traceEvent()` only when copying a selected item for
replacement/source relationships; Blue does not persist or redefine session
events. Consecutive `assistant/chunk` reasoning/text deltas fold into one item
per `turn:step:chunk.type`, matching transcript streaming slots, and retain
their source sequence range. `TracePanel` is mounted through the D30
editor-slot replacement and supports Up/Down item navigation, Enter opening a
stacked full-detail window, PageUp/PageDown scrolling inside that fixed-height
JSON viewport, `c` selected-item copy, `a` full-timeline copy, plus
`/trace copy <seq>` and `/trace copy all`. The selected list row uses the
theme's full-width `selectedBg`; current-surface dots and stream titles use
semantic status colors, while detail JSON keys use the theme accent.
Clipboard output uses the shared OSC52/native `copyTextToClipboard` pipeline.

`src/commands-plugin.ts` registers `/quit` `/resume` `/new` `/fork` `/rewind` `/sessions` `/help` `/init` (`/new`/`/fork` emit the app layer's `blue/request-new`/`blue/request-fork`; `/rewind` projects direct-user turns through `src/rewind.ts` and emits `blue/request-rewind` with the complete `turn/start` boundary; `/sessions` is a persistence-list picker panel; `/help` enumerates `commands.list` + `keymap.list()`). Rewind is deliberately single-level and branch-based: it never truncates the active log, and its panel states that the original remains available. `/theme` (`src/theme-switch.ts`) hot-swaps the `blueTheme` provider via `ctx.registry.delete` + `ctx.plugin`, restoring dark on mount failure. A bare invocation opens the theme picker (D54): every cursor move live-applies the highlighted palette through the shared `SelectListPanel` `onHighlight` callback, Enter keeps it, Escape reverts to the opening theme; without the display quartet the bare command falls back to the `← current` text listing (the selected settings-list row styles in `primary`, not `accent`). Each live swap rebuilds `blue-input`, so the picker re-homes its editor dock slot after every swap and on a deferred `blue/input-editor-changed` emission.

The alias-relation registry `src/command-meta.ts`: `/quit` answers to `/q` and `/exit`, `/new` to `/clear` (S27) — the kimi style: aliases are not registered commands, `blue-input` rewrites an alias line to its canonical command before `ctx.commands.execute`, so the session log and every discovery surface stay canonical-only. Re-registering a canonical replaces its relation; an alias claimed by another canonical fails loud.

`./plugin-host-bridge` is the owner-only public interaction adapter. It maps
additive `bluePluginHost` commands into the real command registry (whose
duplicate gate protects every built-in name), converts `BlueResult` into the
durable command outcome, contains thrown plugin handlers, and sends public
notifications through the current editor's transient notice channel. Its
registrations disappear with either the contributing Fiber or the bridge.

## Dialog mount and framed surfaces (D26 view, D30 mount)

Every dialog surface is a bottom full-width pull-up panel framed with the core `framePanel` chrome. Since D30 the mount is the kimi editor-slot replacement, not a floating overlay: every dialog mounts through `mountEditorReplacement` (see the shared editor seams below) — the panel takes over the editor's dock slot, the editor leaves the tree for the panel's lifetime (buffer, draft, and history survive in the component), only the footer remains below an open panel, and dismissing restores the editor with focus. Dock-mounted panels own their height (the old overlay `maxHeight` bounds are gone). Centered modal popups are rejected unless a surface explicitly calls for one; `showOverlay` stays reserved for genuinely floating surfaces.

The framed surfaces:

- **Approval prompt** (`src/approval-plugin.ts`) — the kimi port: amber `borderFocus` full-width rules, indented `▶ Approve <tool>?` title, numbered choices `N. label` indented under the title with a `▶` pointer on the selected accent row and `textStrong` on the rest, and a `↑/↓ select · 1-4 choose · ↵ confirm` key row (`type feedback · ↵ submit · esc cancel` in feedback mode). Four choices with digit direct-select, session-scoped allowances in a module-level `WeakMap<Agent, Set<string>>`, reject-with-feedback steering, and a FIFO queue keeping one prompt visible.
- **User questions** (`src/questions-plugin.ts` + `src/questionnaire.ts`) — one tabbed overlay per request with a fixed `Other` free-text row; the `question` title, `(○)`/`(✓)` tab markers (two-space separated, active tab and option cursor on `primary`, rows indented two columns), and the `↑↓ select · space toggle · ↵ choose · tab switch · esc cancel` key row. Option descriptions are one-lined and budgeted into the width the label leaves (hidden under five columns — the select-list discipline; #14's process crash was an unbudgeted description overflowing pi-tui's width guard).
- **`/help`** (`src/help.ts` `HelpOverlay`) — a HelpPanel port: `help` title with its key hint, two-column sections (command labels `primary`, key labels `warning`, descriptions `muted`), and a sixteen-row scroll window with a `showing 1-N of M` tail, PageUp/PageDown scrolling ten rows.
- **`/sessions`** — the shared `SelectListPanel` (`src/select-list.ts`; originally a hand-rolled `SessionList` in `select.ts`) with `filter: true` (S30②): scoped to this directory's sessions (normalized exact `cwd === process.cwd()`, no subtree spread — D46), projected by `src/session-tree.ts` into a deterministic lineage tree from the standard `parentSession` header (missing parents and cycles promote to roots), and led by the session title (resolved through the optional `ctx.sessionQuery.readTitleSnapshots` batch read for the newest `DEFAULT_SESSION_TITLE_LIMIT = 100` sessions; an absent service, a failed batch, or a per-session rejection degrades the row to the `id · date` form). The live session's ancestor path is revealed automatically without its ancestors' sibling branches (`▿`); every other branch starts collapsed (`▸`) and Space toggles full expansion (`▾`) while the empty-query title hint says `space toggle branch`. A live search temporarily projects every node so collapsed descendants remain discoverable, then restores disclosure on query clear. The live row keeps `← current`; `fuzzyMatch` searches title + id + date. This is a tree of persisted sessions, not an event cursor inside one session.
- **`BlueSelect`** — frames itself with a `title` option, gives its cursor row the full-width `selectedBg` (the token's first real use) and a `❯ ` pointer, and joins the rest in `textMuted` scroll/key rows.

The shared selector symbols (`SELECT_POINTER`/`CURRENT_MARK`, the kimi `constant/symbols.ts` port) live in `src/symbols.ts`: both list classes draw the pointer from it.

## Model family (S23, D38)

`src/model-commands.ts` registers `/model` (the `ModelPanel` picker over the llm catalog — name/provider columns, `· ctx Nk` metadata, the `← current` badge, the cache-warning row, and the footer thinking-segment control from `src/thinking-segments.ts` drafting the highlighted model's effort with ←/→), `/effort` (the horizontal segment selector with a `Default` segment that omits the effort; `/thinking` is an alias), and `/provider` (`src/provider-add.ts`):

- Active routes open the scoped model picker; dormant pi-ai catalog vendors flash the configuration pointer.
- The `+ Add provider` CTA runs the wizard: known-vendor adoption or a custom endpoint with discovery-fed model adoption, committing through `settings.mutate` on the `llm-pi-ai` namespace (selected explicitly — the configurable directory mixes adapter families) then `credentials.set` under the conventional `<ROUTE>_API_KEY` ref.
- The models step is listing-only: discovery must answer or the add aborts with the classified reason (manual model entry disabled for now). A models.dev catalog pass runs after the models step (`src/models-dev.ts`, the kimi catalog-fetch port: matching the adopted ids against `https://models.dev/api.json` fills each match's context window and pi-ai-gated effort levels; offline quietly falls back), and an optional model-defaults form covers the gap (a context window for models the listing did not describe, and a thinking-effort set written as pi-ai's level→wire map); the discovery probe's classified failures ride the manual form's subtitle.
- The multi-field `src/form-panel.ts` carries Tab/↑↓ field routing, in-panel error lines, and masked bullet rows.
- `src/provider-onboarding.ts` runs once after the loader and first session settle. It gathers credential refs from active routes plus `llm-pi-ai` profiles and calls `credentials.describe` only; when none is configured, a one-field masked form stores `DEEPSEEK_API_KEY` for the existing `deepseek-official` route. Esc skips only the current run and points to `/provider add`; no skip marker is persisted. Known catalog providers never expose Base URL in add/edit forms, while custom endpoints retain the URL and discovery flow.

Every commit writes `blueSession.modelRef.current` (the next step's route); Enter additionally persists via `agentDefaultModel.saveSelection`; Alt+S (`blue.interaction.session-only`, one of three contextual actions beside ←/→ segment steps) commits session-only.

The Alt+M hotkey (`blue.interaction.cycle-model`, S30) cycles the current provider's model list through the same commit path on the session-only channel: `cycleSessionModel` in `src/model-commands.ts` — the next advertised model (wrap-around, 60 s TTL cache over `llm.listModels` so a pressed-in-rhythm hotkey does not re-issue a network listing), no effort carried (the `/model <id>` direct-switch parity), never a persisted default. The editor's `onKey` chain consumes the press before the Editor sees it, so the typed draft survives byte for byte — the point of the hotkey; it fires in bash mode too (input mode and model are orthogonal axes, the Shift+Tab cycle-mode ruling). It is the last key added before the R2 keymap freeze.

## Session-title terminal mirror (S30)

`src/terminal-title.ts` (`blue-terminal-title`, mounted by the package's own `apply`) mirrors the session title onto the terminal window/tab title via `blueScreen.setTitle`: it re-derives on load (late activation), on `'blue/session-changed'`, and on the current session's events — the status-basic event-argument discipline, since a resumed log replays no live events and the fold over `session.events` is the only correct source there. The harness `sessionTitle` service is read through a structural interface (no dsh-session-title dependency); no session, thin host, or untitled session falls back to the product name `blue`. Emission is deduped — a re-derived-but-unchanged title writes nothing. The title itself is generated upstream (the Blue bundle runs the all-prompts cadence); there is deliberately no manual `/title` command (2026-08-22 ruling).

## All-prompts cadence bridge (D41)

`src/session-title-cadence.ts` (`blue-session-title-cadence`, mounted with the baseline plugins) restores the all-prompts cadence the harness service cannot schedule itself: `dsh-session-title@0.1.1-rc.1` starts a pending derivation from the once-per-session `request/header` event or from `onMainRequest`, whose boundary gate (`boundary.seq <= pending.throughSeq`) rejects every message that opened a turn — dsh-agent-loop logs `step/start` before the turn's `user/message` — so from the second message on the title stayed at the first derivation (observed live: a four-message session carried exactly one title request, `messageSeqs [7]`). The bridge calls the public `sessionTitle.refresh(session)` on every human message of the current session, deferred through `queueMicrotask` (the refresh appends, and the observer runs inside `session.append`); before the first request header it stays out — a header-less refresh fails for lack of a route AND supersedes the service's own pending, cancelling both paths. Retire the bridge when the upstream ordering fix lands (checked at the R1 pin move 2026-08-22: 0.1.1-rc.2's dsh-session-title diff is version-line only, the ordering bug is still live, so the bridge stays).

## Session info family (S25)

`src/session-commands.ts` registers `/status` and `/context` (named for CC's context panel rather than kimi's `/usage`) opening the read-only `InfoPanel` (`src/info-panel.ts` — headed two-column label/value rows whose values are styled `InfoSegment` runs, the `/help` scroll window and close keys), and `/version` flashing the banner constant (reached cross-package through the `./banner-content` subpath export). The interaction root's frontend-tree-scoped `displayVersion` config is forwarded into `/status` and `/version` so an acceptance profile can carry a visible identity; absent config uses `BLUE_VERSION`, and neither path mutates release metadata. The numbers come from `sessionProjections.snapshot` (the base composition's token-meter/session-stats folds, replay-correct across resumes — the bundle e2e mounts the real projection family behind the `sessionProjections` boot flag), with `src/usage.ts` as the thin read layer whose pure `assistant/*` fold (replace-per-step, mirroring the upstream unit) answers on projection-less hosts — Blue owns no accumulator. The shared context section renders the kimi `█░` bar severity-colored at 50%/85% with the 1024-base counts, plus the CC-style composition section — the `contextBreakdown` projection's heuristic system/tools/message tokens as a stacked `█▓▒░` bar with component and free rows (sub-half-percent shares read an honest `0%`, the heading carries the heuristic caveat, and hosts without the seam omit the section).

When the optional `blueContextFeature` service is present, `/context` consumes its renderer-neutral `PanelModel` through `src/frontend-panel.ts`. That class is the TUI boundary for generic `select`/`form`/`info`/`loading`/`error` panels: it owns the frame, enabled-item cursor, focus keys, scrolling, submit/cancel action dispatch, loading suppression, and width clamp while the domain feature owns only readonly views and structured actions. Unloading or omitting the feature falls back to the S25 `InfoPanel` path above; no dsh-context type enters this package.

`CommandModelService` is the F5 projection and action boundary over the official `commands` service. It maps `commands.list(agent)` descriptors to immutable `CommandModel` values, subscribes to `commands/change`, and is the only consumer that turns `command.execute` actions back into `commands.execute(...)` calls. Active executions forward caller abort signals; service unload aborts all remaining executions and discards late results. `/help` consumes the projected command list and retains the registry list as its absent-service fallback.

## Export family (S26)

`src/session-export.ts` registers `/export [full] [path]` (writes the current session as Markdown) and `/copy` (pushes the last assistant message's text through `src/clipboard-write.ts`). `/copy` follows the kimi order:

1. **OSC 52 first** — core's `terminal-escape.ts` emitter (`buildClipboardOsc52` + `emitClipboardOsc52`, both legs injectable). The escape is pure stdout output the terminal consumes without rendering, so it neither touches scrollback nor pi-tui's differential frames. It stays bare inside tmux so `set-clipboard on|external` can consume and forward it; DCS passthrough is not used because its separate `allow-passthrough` option is off by default.
2. **Then the platform tools over stdin** (wl-copy → xclip, pbcopy, clip.exe) as the verified path with the paste-image timeout.

A tool win reports `native`; an all-tools-fail with the escape out reports the unverified `copied via terminal escape sequence` notice; both legs dead throws the aggregate `no clipboard tool is available (…)` failure with ENOENT classified as "not installed".

The default export is the readable view — the folded transcript in kimi's export-md shape (front-matter, overview, per-turn `### User`/`### Assistant` sections with `<details>` thinking/tool-result blocks). The `full` mode keyword exports the decoded event stream instead (`buildFullExportMarkdown`, `event_count` front-matter): nothing folded or filtered — the D28 injections appear labeled by `source.kind`, tool results carry their full text, turn/step boundaries and request rows stay visible, unknown event types dump their raw JSON, and only `assistant/chunk` rows stay out (the assembled message carries the whole text).

Both commands share one read path: the persistence backend's raw artifact (`supportsRawArtifacts` + `readRaw`, JSONL chunk rows expanded through dsh's `decodeStorageRecord`, peer+dev dep `@deepseek-ai/dsh-session`) folded by the transcript's `foldSessionEvents` — so the export mirrors exactly what the transcript renders (the D28 injection filter included) and survives compaction and resume. The durable read flushes the session first (`ctx.get('sessions')?.flush`, the SessionStore's documented pre-read channel — the persistence coordinator drains asynchronously), and every guard (no session, no persistence, non-raw backend, no artifact, empty fold, corrupt line, write failure) reports a classified error.

## Light commands (S27)

`src/session-init.ts` registers `/init`: sends the AGENTS.md exploration brief (the kimi `init` registry's spirit, English body) to the UI's current agent as a plain user followup — `agent.followup` on a `source.kind: 'user'` message, so the transcript echoes what was sent — when the agent is idle; refuses with an error notice while it runs or when no session is attached (the `/fork` guard rule). `/clear` joins as `/new`'s alias (the second command-meta consumer): `/clear` completes as `/new (clear)` in the dropdown, `/help` appends the annotation, and the submission rewrites to the canonical name.

## Tools and presets (S28, D37)

- `src/tools-commands.ts` registers `/tools` — a two-step catalog browser over `ctx.tools.schemas(roster.standingKeyFor(composedPreset(agent.ctx)))` under the thin-host roster, the global view otherwise. The first panel is the shared picker (one row per tool with its first sentence as the brief); Enter stacks a read-only detail `InfoPanel` above it: identity rows (name, the MCP server for `mcp__` names), the full description as wrapped source lines, and one row per JSON-Schema parameter with type, description, and a `· required` mark. Escape walks back one panel at a time. Note the cross-store lesson (D37 实施后记): `scopeOf` from a linked Blue's own `dsh-scope` copy reads `undefined` on CLI-tagged contexts — `kScope` is a module-level Symbol and the dev link loads two instances — so the roster's public standing-key API is the cross-store-safe path.
- `src/preset-commands.ts` registers `/preset` — the roster picker over `ctx.agentPresets.list()` with the live composition badged and broken rows disabled. A pick re-dispatches `/preset <id>` through the command runtime (one write path with the typed line); the switch core guards idle + blank (`turn/start` absent; in-process calls have no wire-layer lock), then `recompose(agent.ctx, id)` + the `agent-preset/selected` event append, which is what resume folds to rebuild the composition.

## Skills pipeline (S29)

The semantic model: a line-start `/xxx` stays the strict command domain (unknown commands error, never fall back to the model); a `#name` token anywhere in the line is a skill mark; only the follow-up and Ctrl-S steer paths rewrite recognized marks into the `/name` gesture form. The non-line-start `/xxx` text was always free text through the same follow-up channel.

`src/skills-catalog.ts` is the module-level settled cache of the current session's user-invocable skills (the harness `skills` service read optionally through `ctx.get`, type-only peer `@deepseek-ai/dsh-skill`): settling goes through `snapshot` so an incomplete observation never caches and the last good catalog survives; `skills/change` and `blue/session-changed` drop-and-preheat with an epoch guard against stale in-flight settles.

`rewriteSkillTokens` mirrors the upstream tool-skill gesture boundary verbatim — string start/whitespace before, whitespace/end after, kebab-case names from the settled catalog only — so markdown headings, `[image #N]` markers, uppercase, and unknown tags pass through untouched; the editor history keeps the typed `#name`.

The `blue-editor-plus` `#` completion branch completes the settled skills through the slash dropdown's fuzzy filter: values and prefix carry their `#` so pi-tui's Enter accepts without submitting (the review-then-second-Enter flow); a `user-only ·` description prefix marks skills the model cannot invoke; bash mode declines so a shell `#` comment keeps its Enter; `applyCompletion` replaces the mid-line token with a trailing space.

`/skills` (`src/skills-command.ts`) opens the read-only `InfoPanel` listing the same catalog by folded source layer (project roots under `Project`, user roots under `User`, other sources as delivered) with the name/description/whenToUse rows. The upstream injection arrives as an ordinary `skill-invocation`-sourced session event, which the D28 fold already renders zero rows for — live and replay both. The btw side-question pane's submissions do not run the rewrite (parked).

Acceptance note (2026-08-21): the gesture is invisible on the screen — the echoed user row carries the rewritten `/name` (the typed `#name` survives only in editor history) and the D28 fold renders the skill-invocation injection zero rows, so nothing marks the skill as loaded; the model's answer is the only cue. A visible cue (echoing the typed form, or a one-line dim injection marker as a D28 exception) is parked for post-release — see the roadmap S29 row's 验收注记.

## Shared selectors and permission (S24b, D33)

The shared single-select `SelectListPanel` (`src/select-list.ts`): the center-on-cursor 8-row window, `❯ ` pointer, `← current` badge, `(n/m)` counter, plus the `cycle`/`windowedRange`/`counterRow`/`oneLine` helpers. `/sessions`, `/provider` (whose CTA became a uniform trailing row), the wizard's choose steps, and the `/permission` picker consume it; `BlueSelect` consumes the helpers; `ModelPanel` keeps its own tabbed geometry.

`filter: true` opts a panel into type-to-filter (S30②, `/sessions` the first consumer): printable bytes grow a query matched by `blueComponents.fuzzyMatch` against `row.filterText ?? label` (the optional `filterText` override is the seam titled rows need — the label is the title, the id/date ride the description), the window/counter walk the filtered view, the cursor reseeds on `initialValue`-else-head, Escape clears the query before cancelling (the kimi rule, as in `ModelPanel`), an empty view paints a muted `no matches`, and the empty-query title hint gains a leading `· type to search` fragment. The raw `\x7f`/printable matching mirrors `ModelPanel` — no keymap actions (the freeze holds), and panels that do not opt in keep swallowing printables.

The `/permission` preset picker (`src/permission-panel.ts`, D33): `blue-input` intercepts a bare `/permission` line while `ctx.permissionPresets` is composed (type-only peer dep) and opens the panel; a pick dispatches `/permission <name>` through the command runtime so every switch logs the command and knob events; the danger-full-access row gates behind a typed-`y` `FormPanel` stacked on the picker; the derived `custom` state rides as a blocked display row.

The plan-review intent gets its dedicated panel (`src/plan-review-panel.ts`, the kimi approval shape): `questions-plugin` routes a single-question plan-review ask to the plan markdown in a bordered `plan` box (the btw pane's box idiom) — a viewport-filling window (live `blueScreen.rows` minus the panel chrome, minimum 6) with a showing tail — above the numbered decision list `1. Approve` (label from `intent.approve`) / `2. Reject` (answers with the other option's label) / `3. Revise` (the row carries the inline feedback input in the kimi `3. Revise  <text>` shape: a real editor owns the keys, the row derives from the tracked text with a cursor block and `Type feedback · ↵ submit.` beneath; non-empty submits `{selected: [], custom}`, empty declines plainly). Its height budget leaves one upstream transcript row visible above the panel and two-row footer: `TuiMainScreen` can then differentially repaint a growing `exit_plan_mode` tool result instead of clearing and replaying the viewport on every token. Digits jump-and-fire from the list but type while the input holds focus; Escape dismisses through `ASK_CANCELLED` — the harness-wide code dsh-plan-mode catches, fixing the earlier Blue-invented `ASK_DISMISSED` leak; malformed pairs fall back to the questionnaire.

## Completions (S14, D31)

The pure `src/slash-filter.ts` (the kimi `scoreTokens` port) backs both the provider's slash branch and the hint line's discovery list so the two surfaces agree on what matches: the query splits on whitespace, every token must subsequence-match the command name through `blueComponents.fuzzyMatch`, survivors sort by summed score; the alias extension adds the kimi match rule — the canonical name scores first, aliases count only when it misses, a tie keeps the canonical match ahead, and an alias hit labels the command `/{name} (alias, alias)` while `/help` always appends `(/alias, /alias)`.

Slash items and the returned prefix carry the leading slash so pi-tui's Enter accepts-and-submits and its best-match preselection keys on the typed text; dropdown descriptions join the command's `input.hint` (`hint — description`). Bash mode declines slash suggestions outright (a leading `/` there is a path — with Enter submitting, an unguarded dropdown would run the wrong command).

The `@` branch is the kimi mention composition (D31): empty-tail tokens (a bare `@` or a directory drill-down like `@docs/`) take the deterministic one-level listing from `src/file-mention.ts` (`listDirectoryMentions` — the resolved directory's own entries, directories first then files by `localeCompare`, `.git`/`node_modules` skipped, capped at 50; a deliberate deviation from kimi, whose fd empty-query list re-ranks an arbitrary top-20 cut); query-bearing tokens delegate to `blueComponents.createFileMentionProvider` (scoped queries, substring scoring, top-20, quoted values, fd-respecting-gitignore) while fd is available. `src/file-mention.ts` also supplies the kimi `extractAtPrefix` gate, the `fd`→`fdfind` PATH probe (no managed download), and the fs fallback (2000-entry scan/50-suggestion caps, directories and hidden entries included, `.git` and `node_modules` skipped). `applyCompletion` always delegates (directories keep the cursor for drill-down, files append the trailing space, `@` Enter accepts without submitting); an empty result flashes the hint-line notice `no matching files under the session cwd` (aborted rounds stay quiet); and the core adapter's reopen hook re-opens the dropdown on a bare `@` token or text ending in `/` inside a mention.

The argument-hint ghost drives `setGhostHint` with the kimi `computeArgumentHint` rules: a completed `/command` plus at most one space, the hint lead-spaced until the user types the separator, never in bash, recomputed on re-attach for restored drafts, cleared on detach.

## Editor-plus, shell echo, and pane-queue

The optional `./editor-plus` subpath plugin (`src/editor-plus.ts`, `blue-editor-plus`) layers `!` bash mode — the triple cue of `!` symbol, ` ! shell mode ` border label, and the shellMode hue, re-asserted over the slash resolution while the mode is active — and slash/`@`/`#` autocomplete over the shared editor (the `#` skill branch: see Skills pipeline above), mirroring the input mode into `draft-stash.ts` so a theme-swap reload rebuilds bash.

The shell echo (`ShellExecutor` contract split into `{code, stdout, stderr}` — the node `exec` callback provides them separately; rejections surface as red stderr with code 1) uses the kimi dim presentation: captured output is sanitized by `src/shell-sanitize.ts` (the kimi regex port — CSI/OSC/single-ESC/C0 stripped, `\n`/`\t` kept, never throws) before per-stream caps; the echo header leads with a `shellMode('$ ')` marker followed by the command in the default foreground; stdout and stderr render `textMuted` (stderr `error` on failure) with `(no output)` when both streams are empty; the muted truncation row stays; and the `exit code N` row remains per user ruling (failure stays visible even with silent stderr).

The `./pane-queue` subpath plugin (`src/pane-queue.ts`, `blue-pane-queue`) renders the agent's queued inbox messages (its `↑` glyph accented `primary`) and registers the keyless `blue.queue.recall` action that gates the empty-editor Up recall in `blue-input`.

## Key resolution and btw routing

Overlay key handling resolves through `ctx.blueKeymap`; text-editing keys are owned by the pi-tui Editor; the main editor's contextual keys (Escape/Ctrl-C/Ctrl-S/Ctrl-G) resolve through the editor's `onKey` pre-dispatch hook. Key discipline: a keymap action without a `handler` is contextual (components resolve it via `matches`); a handler-carrying action is global and is consumed by core's dispatcher before focus routing.

Escape's final running-agent branch first asks the app-owned `blueRetractions` service to retract the exact stable message id saved at ordinary follow-up submission. A success restores the original editor value, removes its newest history entry, and runs the reversible submit-transformer rollback (pasted-image markers regain their consumed refs); a refusal uses the historical Interrupted cancellation. Ctrl-C never asks the service. `applyReversibleSubmitTransformers` composes rollback functions in reverse registration order and keeps the legacy block-only helper for other callers.

The side-question pane routes through the editor key chain: while `'blue/editor-connected-above'` is true (the listener mirrors it onto the editor via `setConnectedAbove` and the frame splices `├┤`), Escape closes the pane ahead of the draft clear — the draft survives — and Up/Down with an empty buffer scroll it, both emitted as `'blue/btw-command'`. The keymap's key-level conflict detection (Escape/Up/Down belong to the list surfaces) made a global-action registration impossible, so the editor chain is the sanctioned channel.

## External editor (S31, Ctrl-G)

`src/external-editor.ts` holds the kimi-ported primitives: `resolveExternalEditorCommand` (a non-blank `blue.editorCommand` setting first — the configured layer kimi keeps — then `$VISUAL` > `$EDITOR` > undefined), `quoteShellArg` per platform, and `runExternalEditor` over the injectable launcher hook (`setExternalEditorLauncher`, the clipboard-write pattern). The default launcher seeds a private `mkdtemp` `prompt.md`, spawns `${command} ${quoted file}` with `stdio: 'inherit', shell: true` (argv-style commands like `code --wait` ride the shell), and reads the file back on a zero exit; a nonzero exit — vim's `:cq` among them — resolves `undefined`, so the draft stays untouched with no string sniffing.

The orchestration lives in `blue-input` (`runExternalEditorFlow`, resolved from the `ACTION_EXTERNAL_EDITOR` contextual branch in `handleEditorKey`): a fiber-scoped running flag refuses reentry (the second press is consumed silently), a missing editor only flashes the notice, and the whole edit runs inside `blueScreen.suspend(fn)` — the write-back (`setText` with CRLF collapsed and one trailing newline dropped, then the explicit `currentText`/`stashDraft` mirror re-sync, the recallQueued precedent) happens inside the suspend window so the resumed full frame already shows the edited draft. Boundaries: the seed is `getExpandedText()` — large pastes materialize as full text (the upstream-sanctioned form), while `[image #N]` markers ride as literal text and keep resolving at submit (the paste-image map is module-level and unaffected by `setText`; no degradation, no notice); bash mode survives an external edit unchanged (setText fires no onChange, so the input mode is not re-derived — deleting the `!` prefix externally still needs a manual exit); a fiber unload mid-session leaves the draft alone (`unloaded` guard inside the suspend body).

## Shared editor module seams (`src/editor-instance.ts`)

- **Submit-transformer seam**: `registerSubmitTransformer(fn)` returns a disposer; `applySubmitTransformers(text)` concatenates contributions in registration order (empty result keeps the historical single text block); both the followup and steer paths build content through it.
- **Editor-slot swap**: `setEditorSlotSwap` installs `blue-input`'s machinery on mount and clears it on unload; `mountEditorReplacement(component)` is the dialog-facing entry — it returns an idempotent restore disposer and degrades to a no-op while no input layer is mounted.
- **Draft stash** (`src/draft-stash.ts`): mirrors the unsubmitted editor text, the input mode, and the prompt history (mirrored after every submission, newest-first; `BlueEditor.getHistory()` reads it back through a single structural cast over pi-tui's private field, spec-pinned) so all three survive theme-swap reloads — text restored via `setText`, history replayed through `addToHistory`, on apply; the draft clears on submit/steer while history persists.

## Attachments and paste-image

- `./attachments` (`blue-attachments`, inject `[blueComponents]`) provides the harness `attachments` `AttachmentStore` backed by a filesystem store with magic-byte sniffing as the admission cross-check, a media-type whitelist plus caps (10 MB/image, 8 images/message, 30 MB/message, 16M pixels), files under `DSH_BLUE_ATTACHMENT_DIR ?? $DSH_HOME/attachments ?? ~/.dsh/attachments` as `<uuid>.<ext>`, and `AttachmentError` codes aligned with dsh-attachment's taxonomy. `ADMITTED_IMAGE_TYPES` (the whitelist, in probe-preference order) and `EXT_BY_MEDIA_TYPE` are exported for the sibling paste plugin.
- `./paste-image` (`blue-paste-image`, inject `[attachments, blueKeymap]`) registers the contextual ctrl+v action `blue.image.paste`, resolved in a wrapper on the shared editor's onKey chain. Its validated `backend: auto|wayland|x11` config defaults to `auto`: Wayland leads when `WAYLAND_DISPLAY` exists, X11 leads in a DISPLAY-only session, while strict modes never cross protocols. Each backend lists offered types, reads PNG→JPEG→WebP→GIF, and rejects empty/invalid/mismatched bytes before trying the next representation; direct image bytes always lead. A `text/uri-list` or GNOME copied-files fallback admits an ordered local-file batch through `saveImages`: URL/file APIs only, unique `file:` paths, no final symlinks or non-regular files, bounded reads from the deployment count/per-image/aggregate limits, and magic-byte-derived types/names stripped to basename. The store still owns full decode admission and persistence. A 3 s timeout uses SIGKILL and cools that backend for 60 s under the current display-environment key, surviving theme reloads but automatically retrying after expiry or an environment change. Automatic cross-backend success inserts the marker, then warns which fallback supplied it because a broken Wayland↔X11 bridge can expose stale content. Every press first flashes `pasting image...`; classified failures name the missing tool/session/type/file or raw failure, and late completions after unload no-op (D49).
- `./paste-image` (`blue-paste-image`, inject `[attachments, blueKeymap]`) registers the contextual paste action `blue.image.paste` — ctrl+v plus alt+v on win32, where Windows Terminal/conhost swallow the ctrl+v byte (D55) — resolved in a wrapper on the shared editor's onKey chain. Backends are platform-selected through `backendsFor` (the `setClipboardPlatform` test seam pins it): on Linux the validated `backend: auto|wayland|x11` config defaults to `auto` — and the user layer `blue.pasteImageBackend` (the `/settings` row) overrides the composition backend through `setClipboardBackendOverride` — read from the descriptor's RAW `user` section (the resolved value's `auto` schema default would clobber a configured strict backend), re-synced on every `settings/updated` of the `blue` namespace, and cleared on unload — — Wayland leads when `WAYLAND_DISPLAY` exists, X11 leads in a DISPLAY-only session, strict modes never cross — each backend lists offered types, reads PNG→JPEG→WebP→GIF, and rejects empty/invalid/mismatched bytes before trying the next representation; direct image bytes always lead. win32 runs one `powershell.exe -NoProfile -NonInteractive -STA` spawn whose WinForms `ContainsImage`/`GetImage` (not `Get-Clipboard -Format Image` — raw CF_DIB mishandled) writes a bitmap or a FileDropList staging into a temp dir handed over via the `BLUE_PASTE_STAGE` env (bytes/paths never cross stdout); exit codes 0/2/1, 10 s budget, staged copies are regular files by construction. darwin lists `clipboard info`, reads PNGf/JPEG picture/GIF picture directly (AppleScript writes staging via `open for access`), converts TIFF-only clipboards through `sips` (the Chromium/Electron case; both `«class TIFF»` spellings tried), lists Finder `«class furl»` copies; 5 s budget, and the hex-dump check is deliberately avoided. A copied-file batch is admitted on every platform through the shared `readCopiedPaths` (dedup, deployment count/per-image/aggregate limits, no final symlinks or non-regular files, magic-byte-derived types/names stripped to basename); the store still owns full decode admission and persistence. Timeouts use SIGKILL and cool that backend for 60 s under the current display-environment key, surviving theme reloads but automatically retrying after expiry or an environment change. Automatic Linux cross-backend success inserts the marker, then warns which fallback supplied it because a broken Wayland↔X11 bridge can expose stale content. Every press first flashes `pasting image...`; classified failures name the missing tool/session/type/file or raw failure (the missing-tool text names powershell.exe or osascript on their platforms), and late completions after unload no-op (D49/D55).

## Width discipline (D48)

`tests/fakes.ts`'s width helpers delegate to pi-tui through `../../core/src/width.ts` — no codepoint counters (they let CJK mis-budgets stay green while tripping the real width guard). `tests/width-scan.spec.ts` holds the width contract for the directly constructible panels (FormPanel, ModelPanel, HelpOverlay, InfoPanel, PlanReviewPanel, Questionnaire): each renders every `ADVERSARIAL` fixture at every `SCAN_WIDTHS` with an identity theme (marker paints read as false over-widths) and every row must fit. Fixed furniture that can out-wide a degenerate viewport cuts conditionally: FormPanel below its 20-column floor, ModelPanel's rows below lead+name-cap, framePanel below `FRAME_DEGENERATE_WIDTH` (core chrome) — normal widths emit untouched.

`EditorModelService` projects the mounted prompt editor as a readonly `EditorModel`; it listens to input mount/change events and exposes structured `editor.set`, `editor.submit`, and `editor.abort` actions. Set and submit reuse the shared input owner, including expanded text and the normal submit router; abort reuses the editor's clear/active-request interrupt chain. Every action returns false while the renderer editor is unavailable, and no pi-tui object enters the public model.

`/sessions` waits for the first visible page of title snapshots before mounting, so users do not see session ids replaced by titles. Later pages are loaded lazily in eight-row batches, prefetched immediately after the first page mounts and again once the cursor reaches the middle of the current page, cached by page, and applied to the same `SelectListPanel`; late results are ignored after fiber unload. The temporary `loading sessions...` notice is cleared on every terminal path.
The `blue` namespace consolidation lives in `src/settings.ts` (`blue-settings`, mounted by the package's `apply` ahead of `blue-update-check`, whose boot read consumes the thunk): the tree's ONE `installSettingsSection` registration of `settingsNamespace('blue')` with the extended schema `{ updateCheck: true, updateChannel: 'rc', theme: 'dark', collapseThinking: true, collapseToolCalls: true, windowTurns: 15, recentStepsRetention: 30, expandTurns: 3, userFoldLines: 10, userFoldChars: 1000, editorCommand: '', pasteImageBackend: 'auto' }` — every new key defaulted, so existing settings.yaml files need no migration. Every consumer (the boot update check, the `/settings` panel, the `/update` channel read) resolves the shared thunk `currentBlueSettings()` — schema defaults layered with the user document while a settings service lives, the composition defaults otherwise — instead of registering its own section, so the host sees exactly one `blue` schema.

The input editor also registers a focused-editor scroll handler at the core input boundary. With no dialog/side pane, SGR mouse-wheel events scroll the transcript even while a draft is present; the handler consumes the raw wheel event at both the moving and boundary cases so it cannot become editor history input. Arrows and PageUp/PageDown/End remain transcript controls only for an empty editor, preserving cursor/history behavior for drafts. Core keeps raw wheel events for the primary AltScreen viewport when this handler is absent, while focused replacement panels receive wheel-as-arrow and page keys instead of losing them to the transcript.

## Settings and update seams
`/settings` (`src/settings-command.ts`) is a normally registered command (in `/help` and the completions, not an input-layer interception like `/permission`): a bare invocation opens `SettingsPanel`, the D30 editor-slot mount (`mountEditorReplacement`) in the `framePanel` chrome (`settings` title, `↑↓ select · ↵ change · esc close`) wrapping `components.createSettingsList` — the pi-tui SettingsList adapter's first consumer. Rows are preset cycles (`ns.key` ids): Enter/Space steps the value, a current value outside the cycle is merged in as the selection, and each accepted change writes through `settings.update` (deep-merge) with the descriptor revision — a stale revision (`SettingsConflictError`) re-describes and retries once, anything still failing lands on the panel's own feedback row. `agent-default-model.reasoningEffort: default` instead unsets the key through `settings.mutate`, so the field re-inherits the schema's omission semantics. The row inventory spans `blue.*` (update check, default theme — applied live and persisted, folding defaults), `shell.*` limits, `agent-loop.maxParallelToolCalls`, `web-search-deepseek.maxUses/maxTokens`, and `permission.defaultPreset` (values from the `permissionPresets` service, applies to NEW sessions, row omitted without it); a row whose namespace the host never registered is omitted. The final row `Open settings.yaml in $EDITOR` runs `settings.prepareDocument()` → `$VISUAL`/`$EDITOR` behind `blueScreen.suspend` (the Ctrl-G discipline) and writes the edited text back; no editor configured says so on the feedback row. Refresh is diff-and-update, not rebuild — the core adapter surfaces pi-tui's `updateValue`, so an accepted write only moves the panel's `lastKnown` id→value map (pi-tui's own cycle already displays the value; the commit's `settings/document-updated` emission diffs empty against it and repaints nothing), a failed write rolls the row back through `updateValue`, and a `settings/document-updated` emission (the host file watcher fires it after an external edit) re-describes and pushes only the changed rows' values into the live list; a changed row set (a namespace appeared or disappeared) is the only remount, through the shared `mount` closure. Every row carries a one-line description and the body ratchet-pads to the tallest height seen (plus one constant feedback row), so the frame never resizes as the highlight moves. A theme commit from the panel's own Theme row rebuilds the input fiber mid-swap: the panel re-homes the SAME instance on a deferred `'blue/input-editor-changed'` emission (the `/theme` picker discipline — the list keeps its highlight) and reads its palette through a live getter. A host without the settings service gets the error `settings service unavailable on this host`; all service reads are lazy `ctx.get` (this fiber must never become a theme dependent).
`/settings` (`src/settings-command.ts`) is a normally registered command (in `/help` and the completions, not an input-layer interception like `/permission`): a bare invocation opens the TWO-LEVEL panel over the D30 editor-slot mount (`mountEditorReplacement`) — level one is a `SelectListPanel` of namespaces (`settings` title, `· esc close · ↵ open` hint, one row per registered namespace with an `N settings` tail, plus the trailing `Open settings.yaml in $EDITOR` action row) wrapped in `NoticeTail` (the shared feedback row rendered under the frame, because the open-file action's outcomes land while no level-two panel is mounted); Enter on a namespace stacks level two, a `SettingsPanel` (`settings › <ns>` title, `↑↓ select · ↵ change · esc back` footer) framing `components.createSettingsList` — the pi-tui SettingsList adapter's first consumer — and Escape there pops back to level one. Rows are preset cycles (`ns.key` ids): Enter/Space steps the value, a current value outside the cycle is merged in as the selection, and each accepted change writes through `settings.update` (deep-merge) with the descriptor revision — a stale revision (`SettingsConflictError`) re-describes and retries once, anything still failing lands on the panel's feedback row and rolls the row back through `updateValue`. A row declaring an `unsetValue` (`agent-default-model.reasoningEffort: default`, `llm-deepseek.thinking: default`, `agent-presets.default: none`) cycles into that token and unsets the key through `settings.mutate`, so the field re-inherits the schema's omission semantics; a row marked `editable` (`blue.editorCommand`) instead opens a single-field `FormPanel` for free-form text, an empty submission unsetting the key (its display token is `emptyDisplay`, `auto`). A descriptor declaring `applies: 'restart'` surfaces as a `· restart to apply` description suffix. The row inventory spans `blue.*` (update check/channel, default theme — applied live and persisted, fold defaults, the transcript tunables `windowTurns`/`recentStepsRetention`/`expandTurns`/`userFoldLines`/`userFoldChars`, `editorCommand`, `pasteImageBackend`), `shell.*` (`timeoutMs`/`maxTimeoutMs`/`maxOutputBytes`/`maxSpillBytes`/`graceMs`), `agent-loop.maxParallelToolCalls`, `agent-default-model.reasoningEffort`, `llm-deepseek.thinking`, `web-search-deepseek.maxUses/maxTokens`, and two dynamic rows: `permission.defaultPreset` (values from the `permissionPresets` service) and `agent-presets.default` (value cycle from the `agentPresets` roster's async `list()`; a discovery failure omits the row) — each gated on BOTH its namespace and its value source, and a row whose namespace the host never registered is omitted. The open-file action runs `settings.prepareDocument()` → `$VISUAL`/`$EDITOR` behind `blueScreen.suspend` (the Ctrl-G discipline) and writes the edited text back. Refresh is diff-and-update on the OPEN namespace's list only, not rebuild — the core adapter surfaces pi-tui's `updateValue`, so an accepted write only moves the panel's `lastKnown` id→value map (its `settings/document-updated` emission diffs empty against it and repaints nothing), and an external edit pushes just the changed rows' values into the live list; a changed row set (a namespace appeared or disappeared) remounts both levels (re-seeding the level-one cursor on the namespace the user had open), and a vanished OPEN namespace drops back to level one. Every row carries a one-line description and the level-two body ratchet-pads to the tallest height seen (plus one constant feedback row), so the frame never resizes as the highlight moves. A theme commit from the panel's own Theme row rebuilds the input fiber mid-swap: the panel re-homes the SAME instances (both levels, plus an open form, in stack order) on a deferred `'blue/input-editor-changed'` emission (the `/theme` picker discipline — the lists keep their highlight) and reads its palette through a live getter. A host without the settings service gets the error `settings service unavailable on this host`; all service reads are lazy `ctx.get` (this fiber must never become a theme dependent).
`/settings` (`src/settings-command.ts`) is a normally registered command (in `/help` and the completions, not an input-layer interception like `/permission`): a bare invocation opens the TWO-LEVEL panel over the D30 editor-slot mount (`mountEditorReplacement`) — level one is a `SelectListPanel` of namespaces (`settings` title, `· esc close · ↵ open` hint, one row per registered namespace with a `<blurb> · N settings` tail from `NS_BLURBS` (the host descriptor carries no summary field, so the panel supplies its own one-liners; a namespace without an entry falls back to the bare count), plus the trailing `Open settings.yaml in $EDITOR` action row) wrapped in `NoticeTail` (the shared feedback row rendered under the frame, because the open-file action's outcomes land while no level-two panel is mounted); Enter on a namespace stacks level two, a `SettingsPanel` (`settings › <ns>` title, `↑↓ select · ↵ change · esc back` footer) framing `components.createSettingsList` — the pi-tui SettingsList adapter's first consumer — and Escape there pops back to level one. Rows are preset cycles (`ns.key` ids): Enter/Space steps the value, a current value outside the cycle is merged in as the selection, and each accepted change writes through `settings.update` (deep-merge) with the descriptor revision — a stale revision (`SettingsConflictError`) re-describes and retries once, anything still failing lands on the panel's feedback row and rolls the row back through `updateValue`. A row declaring an `unsetValue` (`agent-default-model.reasoningEffort: default`, `llm-deepseek.thinking: default`, `agent-presets.default: none`) cycles into that token and unsets the key through `settings.mutate`, so the field re-inherits the schema's omission semantics; a row marked `editable` (`blue.editorCommand`) instead opens a single-field `FormPanel` for free-form text, an empty submission unsetting the key (its display token is `emptyDisplay`, `auto`). A descriptor declaring `applies: 'restart'` surfaces as a `· restart to apply` description suffix. The row inventory spans `blue.*` (update check/channel, default theme — applied live and persisted, fold defaults, the transcript tunables `windowTurns`/`recentStepsRetention`/`expandTurns`/`userFoldLines`/`userFoldChars`, `editorCommand`, `pasteImageBackend`), `shell.*` (`timeoutMs`/`maxTimeoutMs`/`maxOutputBytes`/`maxSpillBytes`/`graceMs`), `agent-loop.maxParallelToolCalls`, `agent-default-model.reasoningEffort`, `llm-deepseek.thinking`, `web-search-deepseek.maxUses/maxTokens`, and two dynamic rows: `permission.defaultPreset` (values from the `permissionPresets` service) and `agent-presets.default` (value cycle from the `agentPresets` roster's async `list()`; a discovery failure omits the row) — each gated on BOTH its namespace and its value source, and a row whose namespace the host never registered is omitted. The open-file action runs `settings.prepareDocument()` → `$VISUAL`/`$EDITOR` behind `blueScreen.suspend` (the Ctrl-G discipline) and writes the edited text back. Refresh is diff-and-update on the OPEN namespace's list only, not rebuild — the core adapter surfaces pi-tui's `updateValue`, so an accepted write only moves the panel's `lastKnown` id→value map (its `settings/document-updated` emission diffs empty against it and repaints nothing), and an external edit pushes just the changed rows' values into the live list; a changed row set (a namespace appeared or disappeared) remounts both levels (re-seeding the level-one cursor on the namespace the user had open), and a vanished OPEN namespace drops back to level one. Every row carries a one-line description and the level-two body ratchet-pads to the tallest height seen (plus one constant feedback row), so the frame never resizes as the highlight moves. A theme commit from the panel's own Theme row rebuilds the input fiber mid-swap: the panel re-homes the SAME instances (both levels, plus an open form, in stack order) on a deferred `'blue/input-editor-changed'` emission (the `/theme` picker discipline — the lists keep their highlight) and reads its palette through a live getter. A host without the settings service gets the error `settings service unavailable on this host`; all service reads are lazy `ctx.get` (this fiber must never become a theme dependent).

`src/settings.ts` (`blue-settings`) owns the single `blue` settings namespace with update, theme, and transcript-fold defaults. It exposes `currentBlueSettings()` to the boot update check and `/settings`, applies persisted built-in themes through `theme-switch.ts`, and listens for `settings/updated` without injecting the theme service. `src/settings-command.ts` registers `/settings` as an editor-slot panel over the host SettingsList adapter; rows are preset cycles with one conflict retry, namespace-aware omission, and an external-editor row for settings.yaml. `src/updater/check.ts` consumes the shared settings thunk and never registers a second namespace.
