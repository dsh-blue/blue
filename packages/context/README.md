# `@dsh-blue/blue-context`

Headless dsh-context projection and renderer-neutral `/context` interaction model. It supports replay/resume watermarks, incremental events, absent breakdown fallback, structured open/refresh actions, and Fiber-owned unload.
