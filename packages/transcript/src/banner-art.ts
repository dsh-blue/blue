/**
 * The welcome banner's logo — the compact two-line block mark heading the
 * box, seven columns beside the welcome text. Placeholder art carried over
 * from kimi-code's welcome banner: a Blue-designed mark replaces it here
 * when one exists, and this module stays the logo's single home.
 *
 * @module @dsh-blue/blue-transcript/banner-art
 */

/** The logo: exactly two lines of seven visible columns each. */
export const LOGO_ART: readonly [string, string] = ['▐█▛█▛█▌', '▐█████▌']
